# This monorepo has two separate env files — see:
#   backend/.env.example   (Django settings)
#   frontend/.env.example  (Vite settings)
#
# Copy each to backend/.env and frontend/.env.local respectively.
